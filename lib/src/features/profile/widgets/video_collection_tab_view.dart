import 'package:flutter/material.dart';

class VideoCollectionTabView extends StatelessWidget {
  const VideoCollectionTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Video'),
    );
  }
}
